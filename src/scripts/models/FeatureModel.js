export default class FeatureModel {
    constructor(name, values, specificationUrl) {
        this.name = name;
        this.values = values;
        this.specificationUrl = specificationUrl;
    }
}
